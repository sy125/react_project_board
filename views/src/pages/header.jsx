import React from 'react'
import { Layout } from 'antd';

export default function Header() {
    const { Header } = Layout;
    return (
        <Header className='top'> 
            PAUST   
        </Header>
    );
}
